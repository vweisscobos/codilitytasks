function intersects(center, radius) {
    
}
 
function solution(A) {
    var int = [],
        radius = [],
        comb;

    comb = new Array(A.A);
    
    for (let i = 0; i < A.length - 1; i++) {
        for (let j = i + 1; j < A.length; j++) {
            
        }    
    }
}
